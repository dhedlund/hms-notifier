!!! XML
!!!
%html
  %head
    %title HMS Notifier
    = stylesheet_link_tag 'reset'
    = javascript_include_tag :defaults
    = yield :stylesheets

  %body
    - if content_for?(:content)
      = yield :content
    - else
      %div#content= yield
